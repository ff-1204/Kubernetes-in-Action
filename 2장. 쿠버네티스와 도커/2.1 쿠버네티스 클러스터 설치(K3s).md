# 2.1 쿠버네티스 클러스터 설치(K3s)

## 1. WSL2 환경 준비

먼저 PowerShell(관리자 권한)에서 WSL2와 Ubuntu가 설치되어 있는지 확인합니다.

```powershell
wsl --list --verbose
```

배포판이 하나도 없으면 아래와 같이 안내 메시지가 출력됩니다.

```powershell
PS C:\windows\system32> wsl --list --verbose
Linux용 Windows 하위 시스템 설치된 배포가 없습니다.
아래 지침에 따라 배포를 설치하여 이 resolve 수 있습니다.

사용 가능한 배포를 나열하려면 'wsl.exe --list --online' 사용
설치하려면 'wsl.exe --install <Distro>'를 선택하세요.
```

Ubuntu가 없다면 설치합니다. `--no-launch`는 자동 실행을 건너뛰며, 사용자 계정 생성은 2절에서 진행합니다.

```powershell
wsl --install -d Ubuntu --no-launch
```

설치가 끝났으면 WSL 버전이 2인지 확인합니다.

```powershell
PS C:\windows\system32> wsl --list --verbose
  NAME      STATE           VERSION
* Ubuntu    Stopped         2
```

VERSION이 1로 나온다면 다음 명령으로 변환합니다.

```powershell
wsl --set-version Ubuntu 2
```

## 2. WSL2 내 사전 설정

이후 명령은 모두 WSL2(Ubuntu) 안에서 실행해야 합니다. 먼저 PowerShell에서 `wsl`을 입력해 Ubuntu 셸로 진입합니다.

아래 로그는 **최초 실행 시 사용자 계정 설정**입니다.

```bash
PS C:\windows\system32> wsl
Provisioning the new WSL instance Ubuntu
This might take a while...
Create a default Unix user account: ysmoon
New password:
Retype new password:
passwd: password updated successfully
To run a command as administrator (user "root"), use "sudo <command>".
See "man sudo_root" for details.

ysmoon@YSMOON:/mnt/c/windows/system32$
```

패키지를 업데이트하고 필수 도구를 설치합니다.

```bash
sudo apt update && sudo apt upgrade -y
sudo apt install -y curl
```

## 3. K3s 설치

공식 K3s 설치 스크립트를 실행합니다(v1.35 채널).

```bash
curl -sfL https://get.k3s.io | INSTALL_K3S_CHANNEL=v1.35 sh -
```

**설치 로그**

```bash
ysmoon@YSMOON:/mnt/c/windows/system32$ curl -sfL https://get.k3s.io | INSTALL_K3S_CHANNEL=v1.35 sh -
[INFO]  Finding release for channel v1.35
[INFO]  Using v1.35.4+k3s1 as release
[INFO]  Downloading hash https://github.com/k3s-io/k3s/releases/download/v1.35.4%2Bk3s1/sha256sum-amd64.txt
[INFO]  Downloading binary https://github.com/k3s-io/k3s/releases/download/v1.35.4%2Bk3s1/k3s
[INFO]  Verifying binary download
[INFO]  Installing k3s to /usr/local/bin/k3s
[INFO]  Skipping installation of SELinux RPM
[INFO]  Creating /usr/local/bin/kubectl symlink to k3s
[INFO]  Creating /usr/local/bin/crictl symlink to k3s
[INFO]  Creating /usr/local/bin/ctr symlink to k3s
[INFO]  Creating killall script /usr/local/bin/k3s-killall.sh
[INFO]  Creating uninstall script /usr/local/bin/k3s-uninstall.sh
[INFO]  env: Creating environment file /etc/systemd/system/k3s.service.env
[INFO]  systemd: Creating service file /etc/systemd/system/k3s.service
[INFO]  systemd: Enabling k3s unit
Created symlink /etc/systemd/system/multi-user.target.wants/k3s.service → /etc/systemd/system/k3s.service.
[INFO]  Host iptables-save/iptables-restore tools not found
[INFO]  Host ip6tables-save/ip6tables-restore tools not found
[INFO]  systemd: Starting k3s
```

## 4. K3s 설치 확인

K3s는 자체 kubectl을 포함합니다. 다만 kubeconfig가 root 소유라 일단 `sudo`가 필요합니다(5절에서 풀어줍니다).

```bash
ysmoon@YSMOON:/mnt/c/windows/system32$ sudo kubectl get nodes
NAME     STATUS   ROLES           AGE   VERSION
ysmoon   Ready    control-plane   11s   v1.35.4+k3s1
```

control-plane 노드가 `Ready` 상태로 나오면 정상입니다.

## 5. kubectl 편의 설정 (선택)

매번 `sudo kubectl`을 입력하기 번거로우니 일반 `kubectl`처럼 쓸 수 있게 설정합니다.

```bash
mkdir -p ~/.kube
sudo cp /etc/rancher/k3s/k3s.yaml ~/.kube/config
sudo chown $(id -u):$(id -g) ~/.kube/config
chmod 600 ~/.kube/config
grep -q 'KUBECONFIG=$HOME/.kube/config' ~/.bashrc \
  || echo 'export KUBECONFIG=$HOME/.kube/config' >> ~/.bashrc
source ~/.bashrc
```

- `chmod 600`: kubeconfig에는 클러스터 관리자 인증 정보가 들어 있어, 다른 사용자가 읽지 못하도록 권한을 좁힙니다.
- `grep -q ... || echo`: 셀을 두 번 실행해도 같은 `export` 줄이 `~/.bashrc`에 중복 추가되지 않도록 한 번만 기록합니다.

이제 `kubectl get nodes`만으로 클러스터 조회가 가능합니다.

```bash
ysmoon@YSMOON:/mnt/c/windows/system32$ kubectl get nodes
NAME     STATUS   ROLES           AGE     VERSION
ysmoon   Ready    control-plane   3m47s   v1.35.4+k3s1
```

## 6. 클러스터 상태 확인

### 클러스터 정보 보기

```bash
ysmoon@YSMOON:/mnt/c/windows/system32$ kubectl cluster-info
Kubernetes control plane is running at https://127.0.0.1:6443
CoreDNS is running at https://127.0.0.1:6443/api/v1/namespaces/kube-system/services/kube-dns:dns/proxy
Metrics-server is running at https://127.0.0.1:6443/api/v1/namespaces/kube-system/services/https:metrics-server:https/proxy

To further debug and diagnose cluster problems, use 'kubectl cluster-info dump'.
```

### 모든 네임스페이스에 있는 주요 리소스를 한 번에 조회

> 📝 `kubectl get all`은 이름과 달리 **모든 종류의 리소스**를 보여주진 않습니다. Pod, Service, Deployment, ReplicaSet, StatefulSet, DaemonSet, Job, CronJob, ReplicationController 정도만 포함되며, ConfigMap·Secret·Ingress·PVC 등은 별도로 조회해야 합니다.

```bash
ysmoon@YSMOON:/mnt/c/windows/system32$ kubectl get all -A
NAMESPACE     NAME                                          READY   STATUS      RESTARTS   AGE
kube-system   pod/coredns-c4dbffb5f-cvxqw                   1/1     Running     0          3m51s
kube-system   pod/helm-install-traefik-crd-k4p5w            0/1     Completed   0          3m49s
kube-system   pod/helm-install-traefik-pxwpz                0/1     Completed   1          3m49s
kube-system   pod/local-path-provisioner-5c4dc5d66d-4h7bb   1/1     Running     0          3m51s
kube-system   pod/metrics-server-786d997795-n2kp2           1/1     Running     0          3m51s
kube-system   pod/svclb-traefik-211d3e99-gjnsd              2/2     Running     0          3m32s
kube-system   pod/traefik-9bcdbbd9-2w6m8                    1/1     Running     0          3m32s

NAMESPACE     NAME                     TYPE           CLUSTER-IP      EXTERNAL-IP     PORT(S)                      AGE
default       service/kubernetes       ClusterIP      10.43.0.1       <none>          443/TCP                      4m
kube-system   service/kube-dns         ClusterIP      10.43.0.10      <none>          53/UDP,53/TCP,9153/TCP       3m53s
kube-system   service/metrics-server   ClusterIP      10.43.222.115   <none>          443/TCP                      3m51s
kube-system   service/traefik          LoadBalancer   10.43.181.116   172.26.201.12   80:32055/TCP,443:32362/TCP   3m32s

NAMESPACE     NAME                                    DESIRED   CURRENT   READY   UP-TO-DATE   AVAILABLE   NODE SELECTOR   AGE
kube-system   daemonset.apps/svclb-traefik-211d3e99   1         1         1       1            1           <none>          3m32s

NAMESPACE     NAME                                     READY   UP-TO-DATE   AVAILABLE   AGE
kube-system   deployment.apps/coredns                  1/1     1            1           3m53s
kube-system   deployment.apps/local-path-provisioner   1/1     1            1           3m52s
kube-system   deployment.apps/metrics-server           1/1     1            1           3m51s
kube-system   deployment.apps/traefik                  1/1     1            1           3m32s

NAMESPACE     NAME                                                DESIRED   CURRENT   READY   AGE
kube-system   replicaset.apps/coredns-c4dbffb5f                   1         1         1       3m51s
kube-system   replicaset.apps/local-path-provisioner-5c4dc5d66d   1         1         1       3m51s
kube-system   replicaset.apps/metrics-server-786d997795           1         1         1       3m51s
kube-system   replicaset.apps/traefik-9bcdbbd9                    1         1         1       3m32s

NAMESPACE     NAME                                 STATUS     COMPLETIONS   DURATION   AGE
kube-system   job.batch/helm-install-traefik       Complete   1/1           20s        3m50s
kube-system   job.batch/helm-install-traefik-crd   Complete   1/1           19s        3m50s
```

## 7. 환경 정리 — Ubuntu 통째로 삭제 (선택)

K3s만 따로 지우는 방법(`/usr/local/bin/k3s-uninstall.sh`)도 있지만, WSL2 학습 환경이라면 **Ubuntu 배포판 자체를 통째로 지우는 게 가장 빠르고 확실**합니다. K3s는 Ubuntu 안에서만 살기 때문에, 배포판이 사라지면 K3s·kubeconfig·`/var/lib/rancher` 데이터까지 한꺼번에 정리됩니다.

먼저 WSL을 종료하고 PowerShell(관리자 권한)로 돌아옵니다.

```bash
exit
```

PowerShell에서 등록된 배포판 목록을 확인합니다.

```powershell
wsl --list --verbose
```

Ubuntu를 등록 해제합니다. 이 명령 한 줄로 가상 디스크와 그 안의 모든 데이터가 삭제됩니다.

```powershell
wsl --unregister Ubuntu
```

> ⚠️ `wsl --unregister`는 **되돌릴 수 없습니다.** Ubuntu 안에 둔 코드·매니페스트·PVC 데이터가 모두 사라지므로, 보존할 파일이 있다면 미리 Windows 쪽(`/mnt/c/...`)이나 Git에 백업해 두세요.